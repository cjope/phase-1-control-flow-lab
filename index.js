function scuberGreetingForFeet(result) {
  if (result <= 400) {
    return result = "This one is on me!"
  } else if (result > 2000 && result < 2500) {
    return result = "I will gladly take your thirty bucks."
  } else if (result > 2500) {
    return result = "No can do."
  }
}
//changer result to scubaresponse/depth?

function ternaryCheckCity(destination) {
  if (destination == "NYC") {
    return destination = "Ok, sounds good."
  } else {
    return destination = "No go."
  }
}

function switchOnCharmFromTip(tip) {
  if (tip == "generous") {
    return tip = "Thank you so much."
  } else if (tip == "not as generous") {
    return tip = "Thank you."
  } else {
    return "Bye."
  }
}